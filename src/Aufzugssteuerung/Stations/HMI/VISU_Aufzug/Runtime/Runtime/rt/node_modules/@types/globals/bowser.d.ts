import _Bowser from '../../bowser/index';
export { default } from '../../bowser/index';

declare global {
    /** 
     * This namespace is used to encapsulate all provided Utils libraries.
     */
    namespace SigUtils {
        /** 
         * A small, fast and rich-API browser/platform/engine detector.
         */
        export import Bowser = _Bowser
    }
    interface Window {
        /** 
         * A small, fast and rich-API browser/platform/engine detector.
         */
        bowser: typeof _Bowser
    }
}