declare namespace SigApi {
    interface AlarmSort {
        /**
         * "varno", "alarmnr", "group", "state", "tsHiFirst", "tsHiLast", "tsLo", "tsQuit", "num", "data"
         */
        key: string
        /**
         * sorttype:
         * * "asc" for sorted in ascending order
         * * "desc" for sorted in descending order
         */
        sorttype: string
    }
}
