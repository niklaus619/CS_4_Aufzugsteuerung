declare namespace SigApi {
    interface EventJournalSort {
        key: string
        /**
         * sorttype:
         * * "asc" for sorted in ascending order
         * * "desc" for sorted in descending order
         */
        sorttype: string
    }
}
