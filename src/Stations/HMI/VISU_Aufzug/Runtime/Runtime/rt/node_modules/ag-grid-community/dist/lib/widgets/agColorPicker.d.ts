import { AgDialog } from "ag-grid-community/dist/lib/widgets/agDialog";
import { IAgLabel } from "ag-grid-community/dist/lib/widgets/agAbstractLabel";
import { AgPickerField } from "ag-grid-community/dist/lib/widgets/agPickerField";
interface ColorPickerConfig extends IAgLabel {
    color: string;
}
export declare class AgColorPicker extends AgPickerField<HTMLElement, string> {
    constructor(config?: ColorPickerConfig);
    protected postConstruct(): void;
    showPicker(): AgDialog;
    setValue(color: string): this;
    getValue(): string;
}
export {};
